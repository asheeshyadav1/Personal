# Generated by the img2threejs sculpt pipeline from object-sculpt-spec.json.
# Linting it would mean reformatting output we regenerate, so every rebuild
# would show a diff that says nothing about the model.
